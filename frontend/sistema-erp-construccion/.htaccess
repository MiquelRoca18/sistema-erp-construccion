RewriteEngine On
RewriteBase /sistema-erp-construccion/frontend/

# Redirigir todas las peticiones que no sean archivos o directorios existentes a index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.html [QSA,L]
